/**
 * Add the plugin.
 *
 * @param {any} toolbox - The gluegun toolbox.
 */
async function add(toolbox) {}

/**
 * Remove the plugin.
 *
 * @param {any} toolbox - The gluegun toolbox.
 */
async function remove(toolbox) {}

module.exports = { add, remove };
